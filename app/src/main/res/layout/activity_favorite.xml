<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <android.support.v7.widget.Toolbar
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            android:theme="?attr/actionBarTheme"
            android:minHeight="?attr/actionBarSize"
            android:id="@+id/toolbar" app:subtitleTextColor="@color/white" app:titleTextColor="@color/white"/>

    <ImageView
            android:id="@+id/background_pic"
            android:layout_width="250dp"
            android:layout_height="250dp"
            android:alpha="0.2" android:layout_centerInParent="true" android:src="@drawable/ic_favorite_black_24dp"
            android:contentDescription="@string/logo"/>
</RelativeLayout>